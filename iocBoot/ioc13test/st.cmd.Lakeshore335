errlogInit(5000)
< envPaths
# Tell EPICS all about the record types, device-support modules, drivers,
# etc. in this build from CARS
dbLoadDatabase("../../dbd/CARSLinux.dbd")
CARSLinux_registerRecordDeviceDriver(pdbbase)

# This is for local serial port
drvAsynSerialPortConfigure("serial1", "/dev/ttyUSB0", 0, 0, 0)
asynSetOption("serial1", 0, "baud", "57600")
asynSetOption("serial1", 0, "bits", "7")
asynSetOption("serial1", 0, "parity", "odd")

asynOctetConnect("serial1", "serial1")
asynOctetSetInputEos("serial1",0,"\r\n")
asynOctetSetOutputEos("serial1",0,"\r\n")

epicsEnvSet("STREAM_PROTOCOL_PATH","$(IP)/db")

# Load asynRecord records on all ports
dbLoadTemplate("asynRecord.substitutions")

dbLoadRecords("$(IP)/db/LakeShore335.db", "P=13TEST:,Q=A,PORT=serial1,ADDR=0")

iocInit

